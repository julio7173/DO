import { InittopDirective } from './inittop.directive';

describe('InittopDirective', () => {
  it('should create an instance', () => {
    const directive = new InittopDirective();
    expect(directive).toBeTruthy();
  });
});
