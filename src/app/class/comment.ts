export class Comment {
    top: number;
    heigh: number
    comm: string;

    constructor(top: number, heigh: number, comm:string){
        this.top = top;
        this.heigh = heigh
        this.comm = comm;
    }
}
